const basevalue=prompt('Enter the base of triangle:');
const heightvalue=prompt('Enter the height of triangle value:');
//calculate the area
const areavalue=(basevalue*heightvalue)/2;
console.log('Teh area of the triangle is ${areavalue}');
